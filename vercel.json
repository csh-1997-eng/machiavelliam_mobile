{
  "buildCommand": "flutter build web --release --dart-define=API_BASE_URL=https://$VERCEL_URL",
  "outputDirectory": "build/web",
  "installCommand": "npm install",
  "functions": {
    "api/*.ts": {
      "runtime": "nodejs20.x"
    }
  }
}
